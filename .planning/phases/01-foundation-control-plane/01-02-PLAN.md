---
phase: 01-foundation-control-plane
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/ui/app-shell.tsx
  - src/ui/routes/provider-config.tsx
  - src/ui/routes/schedule-config.tsx
  - src/ui/routes/manual-runs.tsx
  - src/ui/routes/run-history.tsx
  - src/ui/components/run-status-badge.tsx
  - src/ui/components/run-details-drawer.tsx
  - src/ui/lib/api-client.ts
  - src/ui/styles/tokens.css
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
requirements_addressed:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
must_haves:
  truths:
    - "Operator can manage provider profiles and set active profile from UI with activation feedback (D-09, D-12)."
    - "Operator can manage schedule config and see timezone fixed to Asia/Jakarta (D-05, D-08)."
    - "Operator can trigger weekly/daily runs manually and see run-in-progress rejection feedback (D-07)."
    - "Operator can inspect run history and details with required status vocabulary (D-13)."
  artifacts:
    - path: "src/ui/routes/provider-config.tsx"
      provides: "Provider profile management screen"
      min_lines: 100
    - path: "src/ui/routes/schedule-config.tsx"
      provides: "Schedule control screen"
      min_lines: 80
    - path: "src/ui/routes/run-history.tsx"
      provides: "Run history table and details drawer"
      min_lines: 120
  key_links:
    - from: "src/ui/routes/provider-config.tsx"
      to: "src/modules/provider/profiles.routes.ts"
      via: "HTTP client mutations and activation flow"
    - from: "src/ui/routes/schedule-config.tsx"
      to: "src/modules/schedule/schedule.routes.ts"
      via: "Read/write schedule config"
    - from: "src/ui/routes/manual-runs.tsx"
      to: "src/modules/runs/runs.routes.ts"
      via: "Manual trigger actions and conflict handling"
---

<objective>
Implement the Phase 1 operator-facing control plane UI and wire it to wave 1 APIs with the locked UI contract.
</objective>

<context>
- `.planning/phases/01-foundation-control-plane/01-CONTEXT.md`
- `.planning/phases/01-foundation-control-plane/01-UI-SPEC.md`
- `.planning/phases/01-foundation-control-plane/01-01-PLAN.md`
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build shell, tokens, and route scaffolding per UI-SPEC</name>
  <files>src/ui/app-shell.tsx, src/ui/styles/tokens.css, src/ui/lib/api-client.ts</files>
  <behavior>
    - Tabs include Provider Config, Schedule, Manual Runs, Run History.
    - Typography and color tokens follow 01-UI-SPEC contract.
    - Status badge token set includes all lifecycle values from D-13.
  </behavior>
  <action>Define UI shell layout and tokenized style system from `01-UI-SPEC.md`. Keep accent color usage limited to allowed elements and preserve specified copywriting strings for empty/error states.</action>
  <verify>
    <automated>npm run test -- --runInBand ui-shell tokens</automated>
  </verify>
  <done>UI foundation compiles and enforces the approved design contract without ad hoc token drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement provider and schedule configuration screens</name>
  <files>src/ui/routes/provider-config.tsx, src/ui/routes/schedule-config.tsx</files>
  <behavior>
    - Multiple profiles can be created/edited and one set active.
    - Activation action surfaces provider ping validation errors from API.
    - Schedule screen displays fixed timezone label `Asia/Jakarta` and weekday-only setup.
  </behavior>
  <action>Wire forms to wave 1 endpoints with strict client-side + server response handling. Ensure destructive/disable confirmations use exact UI-SPEC copy contract.</action>
  <verify>
    <automated>npm run test:integration -- provider-config schedule-config</automated>
  </verify>
  <done>Operator can fully manage config/schedule through UI with compliant copy and policy feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement manual run and history observability screens</name>
  <files>src/ui/routes/manual-runs.tsx, src/ui/routes/run-history.tsx, src/ui/components/run-status-badge.tsx, src/ui/components/run-details-drawer.tsx</files>
  <behavior>
    - Manual weekly/daily trigger actions call APIs and handle 409 run-in-progress state.
    - Run history shows metadata baseline (trigger type, schedule type, timestamps, status, duration, error summary).
    - Details drawer displays concise error context from API.
  </behavior>
  <action>Connect trigger/history routes to run APIs from wave 1. Disable trigger CTA while active run state is present and display helper text `Run in progress` per UI-SPEC.</action>
  <verify>
    <automated>npm run test:integration -- manual-runs run-history</automated>
  </verify>
  <done>UI delivers full operator visibility and control plane interactions for Phase 1 scope.</done>
</task>
</tasks>

<verification>
- Run `npm run lint`
- Run `npm run test -- --runInBand`
- Run `npm run test:integration -- provider-config schedule-config manual-runs run-history`
- Run `docker compose up -d && npm run smoke:phase1 && docker compose down`
</verification>

<success_criteria>
- UI/API management surfaces exist for provider and schedule settings.
- Manual trigger workflow and conflict handling are operator-visible.
- Run history and detail views satisfy PLAT-04 observability baseline and UI-SPEC contract.
</success_criteria>
