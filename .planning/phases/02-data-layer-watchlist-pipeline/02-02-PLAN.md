---
phase: 02-data-layer-watchlist-pipeline
plan: 02
type: execute
wave: 2
gap_closure: true
depends_on:
  - 02-01
files_modified:
  - src/db/schema.sql
  - src/modules/data/universe.repository.ts
  - src/modules/data/universe.service.ts
  - src/modules/data/watchlist.repository.ts
  - src/modules/data/watchlist.service.ts
  - src/modules/data/data.routes.ts
  - src/modules/runs/runs.routes.ts
  - src/server.ts
  - src/ui/app-shell.tsx
  - src/ui/routes/watchlist.tsx
  - src/ui/public/app.js
  - src/ui/public/app.css
  - tests/data/universe-crud.integration.test.ts
  - tests/data/watchlist-crud.integration.test.ts
  - tests/ui/watchlist-ui.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
requirements_addressed:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Operator dapat melihat daftar LQ45 dan watchlist aktif dari UI."
    - "Operator dapat tambah/edit/hapus simbol LQ45 melalui UI/API dengan validasi simbol."
    - "Operator dapat tambah/edit/hapus watchlist manual override dengan mode guard yang jelas."
    - "UI menyediakan tombol eksplisit untuk `Sync LQ45 Universe` dan `Run Weekly Selection`."
    - "Semua action menulis audit trail minimal ke run logs / error logs yang sudah ada."
  artifacts:
    - path: "src/modules/data/data.routes.ts"
      provides: "Universe + watchlist CRUD + action endpoints"
      min_lines: 140
    - path: "src/ui/public/app.js"
      provides: "Operator flows for universe/watchlist management and action buttons"
      min_lines: 180
    - path: "tests/data/universe-crud.integration.test.ts"
      provides: "API-level CRUD and validation regression coverage"
      min_lines: 80
  key_links:
    - from: "src/ui/public/app.js"
      to: "src/modules/data/data.routes.ts"
      via: "CRUD and action button HTTP calls"
    - from: "src/modules/data/data.routes.ts"
      to: "src/modules/runs/runs.routes.ts"
      via: "Weekly run trigger bridge for explicit UI action"
---

<objective>
Close Phase 2 UAT gap by delivering operator-facing LQ45/watchlist management interface and explicit weekly orchestration actions (sync + run).
</objective>

<context>
- `.planning/phases/02-data-layer-watchlist-pipeline/02-UAT.md`
- `.planning/phases/02-data-layer-watchlist-pipeline/02-REVIEW.md`
- `.planning/phases/02-data-layer-watchlist-pipeline/02-01-PLAN.md`
- `.planning/phases/02-data-layer-watchlist-pipeline/02-UI-SPEC.md`
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add Universe CRUD + sync endpoint</name>
  <files>src/modules/data/universe.repository.ts, src/modules/data/universe.service.ts, src/modules/data/data.routes.ts</files>
  <behavior>
    - List current LQ45 symbols from persisted universe snapshot.
    - Add/edit/delete symbol operations with strict ticker validation and dedupe.
    - Explicit `POST /api/universe/sync` action to refresh and persist latest universe snapshot.
  </behavior>
  <action>Implement repository/service CRUD helpers and expose endpoints with 4xx validation responses and deterministic ordering.</action>
  <verify>
    <automated>npm run test -- --runInBand universe-crud</automated>
  </verify>
  <done>Universe can be managed directly from control plane and refreshed via explicit sync action.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add watchlist manual CRUD/override endpoints with guard mode</name>
  <files>src/modules/data/watchlist.repository.ts, src/modules/data/watchlist.service.ts, src/modules/data/data.routes.ts</files>
  <behavior>
    - Operator can add/edit/remove watchlist symbols for current active watchlist.
    - Manual override requires explicit mode toggle to avoid accidental drift from scoring pipeline.
    - Rank updates remain deterministic and re-normalized after edits/deletes.
  </behavior>
  <action>Implement watchlist mutation endpoints and guard behavior (`manual_override_enabled`) plus validation for symbol existence and duplicates.</action>
  <verify>
    <automated>npm run test -- --runInBand watchlist-crud</automated>
  </verify>
  <done>Watchlist can be corrected/edited from UI while preserving operational safety constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose explicit action endpoints for weekly run and universe sync</name>
  <files>src/modules/data/data.routes.ts, src/modules/runs/runs.routes.ts, src/server.ts</files>
  <behavior>
    - `Sync LQ45 Universe` button action endpoint available.
    - `Run Weekly Selection` button action endpoint available and reuses run admission/conflict rules.
    - Action responses are operator-readable with concise status/result payload.
  </behavior>
  <action>Bridge data module actions with runs module trigger path and ensure 409/validation errors are surfaced cleanly.</action>
  <verify>
    <automated>npm run test:integration -- weekly-action universe-sync</automated>
  </verify>
  <done>Operator can execute expected weekly operational flow directly from dedicated controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Build Universe & Watchlist UI screen and wire controls</name>
  <files>src/ui/app-shell.tsx, src/ui/routes/watchlist.tsx, src/ui/public/app.js, src/ui/public/app.css</files>
  <behavior>
    - Screen shows LQ45 universe list + watchlist list side-by-side.
    - CRUD actions (add/edit/delete) available for both lists with validation feedback.
    - Buttons present: `Sync LQ45 Universe`, `Run Weekly Selection`.
  </behavior>
  <action>Implement end-to-end UI flows and hook to new APIs while preserving existing Phase 1 shell behavior and tests.</action>
  <verify>
    <automated>npm run test -- --runInBand watchlist-ui</automated>
  </verify>
  <done>User-reported UAT gaps are directly resolved in interface and interaction flows.</done>
</task>
</tasks>

<verification>
- Run `npm run build`
- Run `npm run test`
- Run `npm run test:integration -- weekly-action universe-sync`
- Manual UAT retest for Phase 2 scenario in `02-UAT.md`
</verification>

<success_criteria>
- User can view and manage LQ45 universe and watchlist from UI.
- User can execute explicit `Sync LQ45 Universe` and `Run Weekly Selection` actions.
- Phase 2 UAT Test 1 changes from FAIL to PASS with evidence.
</success_criteria>
